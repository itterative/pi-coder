[simple]
agent spawn (worker) — Implement fix

[detailed]
agent spawn (worker) — Implement fix

Agent worker-1 started in the background. Progress and the final result will be delivered asynchronously. Do not
duplicate the same investigation in the parent unless you intentionally want overlapping work. If you have no other work
to do, report your current progress to the user and end your turn. Do not sleep or poll; an automatic notification will
arrive when the run finishes or needs parent guidance. After a terminal notification, retrieve the full result with
agent(action="collect", runId="worker-1").

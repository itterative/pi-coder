# Compaction trace report

Every compaction the extension owns appends a few JSONL records so a pipeline whose intermediates never reach
the session transcript can still be examined after the fact. The writer is `src/modules/compaction/trace.ts`
(`.state/compaction-trace.jsonl`, rotated into `traceGenerations` sibling segments and read back oldest first);
the reader is `scripts/compaction-report.ts`, run through tsx because it shares `src/common/record-log.ts` with
the writer.

## What is recorded

All records of one compaction share a run id, which is what lets the report group them.

| Stage            | What it carries                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ---------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `prefix`         | how the rebuilt stage-1 request compared to what pi actually sent, answered by the retained hash ladder: `reference` (`chain`/`none`), `prefixUsable` (**absent**, never false, when nothing was comparable), `commonPrefixMessages` = deepest reference depth that agreed, `comparableDepth`, `referenceDepth`, `observations` (comparable), `chainObservations` (held), `branchObservations` (on this branch), `verifiedThrough`, `truncated`, `parameters[]`, `historyTruncated`, `modelDivergence`, `unknowns[]`, and fingerprints of our own request and of the newest on-branch parent request                                                  |
| `attempt`        | one per strategy: the request-side numbers (`messageCount`, `estimatedTokens`, `estimateSource` (`exact-cut` / `exact-anchor` / `usage-anchor` / `head-ledger` / `chars4`), `staleAnchors`, `foldCorrected`, `chosenFirstKeptEntryId`, `proposedFirstKeptEntryId`, `reportedContextTokens`, `contextWindow`, `maxTokens`, `toolCount`, `copiedEntries`, `cutFound` (stage 1), `serializedChars`, `droppedBlocks`, `segmentSummaryChars`, `previousSummaryChars`, and stage 1's nested `ledger` object), how it ended (`accepted` / `rejected` / `skipped` with `detail`), with `usage`, `stopReason` whenever a reply arrived, `cause`, and `retries` |
| `model_response` | what the model answered with, before the harness appended anything                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `final_summary`  | the exact persisted text, with `firstKeptEntryId`, `tokensBefore`, `summarizedMessages`, `droppedBlocks`, and the file counts the appended sections were built from                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `outcome`        | how the whole compaction ended: `two-stage`, `native`, `serialized`, `core-default`, `cancelled`, `abandoned` (stopped on purpose: the cause named the account, not the request), `disabled`                                                                                                                                                                                                                                                                                                                                                                                                                                                          |

Costs are development numbers only, and they are counted twice by design: a stage-1 resend shows up here as
`usage.input`, and the same tokens may also be counted on the parent's next turn elsewhere.

## Where the reference comes from

`chain.ts` folds a cumulative hash over every message array pi sends, keeps it in memory keyed by session
manager, and persists it to the trace file. It costs about seventy bytes per request instead of a two-megabyte
body. The report reads only the verdict the chain wrote into the `prefix` record; see the `compaction` memory for
why comparison happens at the reference's depths and why an absent reference must never be printed as a failed
one.

Three counts describe how much of that store could be used, and they are reported separately because each zero
means something different:

| field                | meaning                                                                                        | what a zero says                                                                                            |
| -------------------- | ---------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| `chainObservations`  | entries held for this session, restored rows included (a floor: the scan stops once satisfied) | no readable row for this session survived in the retained file - never written, rotated away, or unreadable |
| `branchObservations` | entries whose leaf sits on the current branch                                                  | entries exist, but they belong to a branch that was navigated away from                                     |
| `observations`       | entries that also share our system prompt and tool set                                         | the branch's requests were built under a different prompt or tool set than ours                             |

`prefix.parentRequest` carries the newest on-branch entry's shape — model, system-prompt length and hash, tool
hash, depth, leaf id — as the twin of `prefix.ourRequest`, so "did our rebuild send what pi sends" is a field
comparison rather than an inference. Both sides hash the whole system text: `ourRequest.systemHash` used to hash
a 320-char excerpt, which reported no change across a prompt that had grown from 12,817 to 24,813 characters.

Every record carries `instance` besides `ts` and `id`, and the header prints the newest one along with how many
older loads contributed to the file. Two reloads can leave rows in one log whose chains, config, and registered
handlers were never the same, and before this field that was unknowable from the data - it had to be reasoned back
in from timestamps and inference, which is how a wrong story survives long enough to be acted on.

One guarantee is worth knowing before reasoning from these numbers: `reference: "none"` is only reachable when
`branchObservations` is zero, and `"chain"` with `observations: 0` is the shape-mismatch case. That distinction
settled a live question about whether an empty verdict meant a cold store or a filtered one, and the report now
checks it as an invariant instead of leaving it to be derived.

## The persisted chain

Persistence is what turns "this process is young" into a statement about the session. Two kinds of row go into
the trace file, gated by `chainTraceEnabled` (default on) rather than by body tracing:

| stage           | written                                                 | carries                                                                                      |
| --------------- | ------------------------------------------------------- | -------------------------------------------------------------------------------------------- |
| `chain_request` | every provider request pi makes, including a child's    | the head at that request's depth, its leaf id, and the shape: system hash, tools hash, model |
| `chain_ladder`  | when the span has grown 32 messages, or on shape change | the heads at every depth of one request, as a dense array                                    |

The ladder rows are what a truncated stage-1 span needs: per-request heads all sit at depth N or deeper, so a
span cut to 64 messages meets none of them. Reading back walks segments newest-first, stops once it holds a
ladder and 32 requests, and ends the walk on a `stat` when a segment's mtime predates the session's first entry -
so the cost tracks what the session needs rather than how much history exists. Because the walk stops as soon as
it is satisfied, the counts a verdict carries are a floor rather than a census, and `unknowns[]` states when that is
the case - which also means an empty chain is a statement about the retained file window, not about all history.

These rows contain hashes, depths, and entry ids and never message content, which is why they have their own
switch: with bodies off, history still accumulates for the next time tracing is on. The verdict itself is only
recorded while the trace is enabled, so the switch buys continuity of evidence rather than an independent way to
check stage 1. `COMPACTION_CHAIN_TRACE=0` turns the rows off on its own, and the report counts what remains on a
`chain:` header line instead of folding them into run records, since they belong to no single compaction.

## Reading it

```bash
npm run compaction-report                                     # newest 10 runs plus the aggregates
npm run compaction-report -- --runs 0                         # every run in the file
npm run compaction-report -- --suspect --session 01a070e0     # only the runs carrying a flag
npm run compaction-report -- --dump=native --runs 1           # stage 1's answer, verbatim
npm run compaction-report -- --grep bwrap --dump=final        # find a run by its text, read what persisted
npm run compaction-report -- --json | jq '.aggregates.flags'  # machine-readable
```

`--dump` prints stage text unindented and untruncated, because a checkpoint is markdown and the report body
only ever shows a one-line preview. Stages are `native` (stage 1's answer), `serialized` (stage 2's answer),
`final` (what was persisted), or `all`.

The report body renders one block per run: the prefix verdict, one line per stage attempt with its request
numbers and `in/cached/cw/out`, the persisted summary's size and cut point, and any flags. `+Ns` after a
stage is the gap to the previous record, which is as close to per-stage latency as the trace gets.

The prefix line adds `chain=held/branch/comparable` and `sys=<our chars>c`, plus `parent-sys=<chars>c` when a
branch reference existed, because those pairs decide whether the verdict on that same line means anything. When
`comparable` is zero the line adds `rej=<n>sys/<m>tools`: which half of the shape filter rejected the entries,
counted per gate and printed together so a zero reads as one gate staying innocent rather than unmeasured. `div=`
now carries value differences too (`enable_thinking:true!=false`), which `params=` cannot show: a key set says both
bodies sent the field, and only the values know they meant opposite things by it. `PREFIX DECODE VALUES` tallies
the same pairs across runs, skipping `max_completion_tokens` — stage 1 caps its output by design, so that one
differs on every run and a table row for it would be noise with a name. A run
can also print `~ cannot tell:` lines: statements from the record about what it cannot answer, kept apart from
flags because they describe the instrument's reach rather than the run's behaviour. `chain-empty` beside
`cache-read-zero` is the pair that means "we do not know whether the cache was reused", and the record now says
so rather than leaving a zero to be read as a measurement. The same rule reaches the decode values: a credited
reference recorded by an older build carries none of them, and prints
`decode values unknown: the credited reference was recorded before these fields existed` instead of leaving the
missing comparison to read as a passing one.

`INVARIANTS` lists cross-field checks — an empty chain that reports entries on the branch, `reference=none` with
entries to compare, a usable verdict with no comparable depth, matching hashes over differently sized prompts, a
disagreement printed at or above the depth its own reference was credited through, an emptied funnel that neither
rejection counter accounts for, and a stage-1 attempt that names `estimateSource: head-ledger` without carrying the
`ledger` numbers that tier is made of. A violation there is a bug in
the trace, not in the cache, which is the distinction that has to be made before any other conclusion in this file
is worth anything.

The last of those is worth stating on its own, because it was violated by the instrument rather than caught by it:
heads are cumulative, so within one reference a mismatch can only sit deeper than that reference's agreement.
`verified=728/728` beside `first=messages[59]` could therefore only ever have meant two references merged into one
verdict, which is what the record used to do. Now `referenceLeafId` names the single request the depths belong to,
`ref=ladder` says the answer came from a retained ladder rather than a request at that depth, and `elsewhere=N`
counts how many other comparable references disagreed somewhere — visible, but no longer able to move a number.

`CAUSES` aggregates `strategy outcome cause`, and `ATTEMPT FAILURES` keys each row by its cause
(`native rejected [quota]: ...`) before normalizing the message. That ordering matters: the two failures that
look most alike in free text — context overflow and an exhausted account — call for opposite responses, so a
table that groups them hides the one number that decides it. A skipped stage-2 attempt reading
`not attempted: <cause>` is the trace's way of distinguishing a deliberate stop from a missing budget.

### How stage 1 sizes its own request

`est=` is printed beside `src=`, because the number means nothing without the method that produced it. Five
states, best evidence first:

| `src=`         | where the number came from                                                                                                                                                                                                                                                                                                                   | band                                                                                                 |
| -------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| `exact`        | the provider's count of the request whose body **was** this span: the reply sitting at the cut point charged for the system prompt, the tools, and every message before it - which is exactly what stage 1 re-sends. Nothing about this body is estimated.                                                                                   | 5%                                                                                                   |
| `exact-anchor` | the newest count _inside_ the span with **nothing after it left to charge**: the whole-turn cut `[assistant]                                                                                                                                                                                                                                 | [user]`, where the span ends on the counted reply itself, so the anchor reaches the end of the body. | 5%  |
| `anchor`       | the newest usable provider count _inside_ the span, plus chars/4 for what landed after it and for the instruction.                                                                                                                                                                                                                           | 15%                                                                                                  |
| `ledger`       | no count inside the span survived, but the session solved for the **head** no row owns — the system prompt, the tool definitions, and the newest checkpoint — from the first reply a provider counted under it, then bracketed the rows between two counts (`ledger.ts`). Arithmetic over counts rather than a ratio applied to the content. | 5%                                                                                                   |
| `chars4`       | chars/4 over the whole body. The only tier available right after a fold, before any post-fold reply exists.                                                                                                                                                                                                                                  | 50%                                                                                                  |
| `unrecorded`   | this record predates the field. Never read it as any of the above.                                                                                                                                                                                                                                                                           | -                                                                                                    |

`cut=` says whether core's boundary resolved (`found` / `missing` / `unrecorded`), and `cutMoved=` says what
this module did with it: `no` when the proposed boundary was admissible and fits, `to:<id>/<N>r` when the walk moved
the span **earlier** (with the row count, because "a move" and "how far" are different facts), and `unrecorded` on a
record from before the walk exists. The earlier-only direction is what makes a move safe to leave out of the
summary's own accounting: the retained tail only grows, so `keepRecentTokens` still holds, and core's list of
summarized messages stays a superset of what was actually dropped. The one field a move does not change is
`tokensBefore`, which sizes the whole live context rather than the span despite its name.

A move or an abstain gets a second line, `cut:`, because two ids cannot explain either:

```
                  cut:      core=tail-under-keep-budget tail=20.8k/20k span=178k basis=count live=context-usage
```

| token          | what it answers                                                                                                                                                                                                                                                           |
| -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `core=`        | the condition that refused **core's** boundary, and therefore the cause of any move. One of the six rejection names; absent when nothing refused it - which is what `cutMoved=no` with no `cut:` line at all means.                                                       |
| `tail/keep`    | retained history at the chosen boundary against the user's floor. The pair, because a tail is meaningless without the number it was weighed against: `20.8k/20k` cleared the floor by 752 tokens on the run this line came from.                                          |
| `span=`        | the chosen span's size - always a measured body, never a chars/4 guess.                                                                                                                                                                                                   |
| `basis=`       | which route measured it: `count` is a provider's own count of that very body, `ledger` is arithmetic over the counts that bracket its rows, which is what makes a position below a fold repairable at all.                                                                |
| `live=`        | which instrument sized the whole live context this tail was subtracted from: `ledger` means both sides of that subtraction are head-plus-rows, `context-usage` means pi's hybrid answered - a provider count through the last reply plus chars/4 for everything after it. |
| `refused(...)` | per-condition tallies, so a run that could not be repaired says what stopped it instead of only saying it could not.                                                                                                                                                      |

The `CUT` section aggregates this: attempts carrying a decision, how many moved, and a histogram of `core=` causes.
Read that histogram before concluding anything about the repair path, because a move is not automatically a repair:
three of the six conditions are about our instruments (the row carries no count, its count expired, the ledger
refused to vouch for that much chars/4), and only `tail-under-keep-budget` and `span-does-not-fit` are about the
user's constraint or the window. `--json` carries all of it under the record's own field names.

`corrected=` appears beside `stale=` only when non-zero: how many folds vouched for their own arithmetic
(`countedBodyTokens` net of `fixedPrefixTokens`, plus their `usage.output`) and turned a rejected count back
into the tier's number. A rescued count keeps the `usage-anchor` label, so `corrected=` is what separates
a count plus a guessed tail from a count corrected through a fold, plus a guessed tail.

`stale=` appears beside them only when non-zero: assistant rows that carried a count but were rejected because
the count predates a compaction or a model/thinking change. That field is what separates "this session was never
counted" from "the only counts here expired", which is the state a session sits in immediately after a fold - and
the state in which overflow compaction tends to fire again.

### What the head ledger is worth

Every request pi sends is `[system prompt][tools][checkpoint(s)][rows...]`, and the first three are one quantity —
the head — that sits in every request and in no row, so no stored count prices it. The ledger recovers it as a
difference: solve `head = input(A) - t(first kept .. A)` against the first reply a provider counted after the
newest fold, then size a body as head plus rows plus the appended instruction, where most of those rows are
themselves bracketed by two counts. `estimateSource: "head-ledger"` prints as `src=ledger`.

The record carries the derivation's own breakdown as a nested `ledger` object on the stage-1 attempt — `tokens`,
`estimatedTokens`, `headTokens`, `headSource` (`first-reply` or `after-fold`), `headEstimatedTokens`, `rowsCounted`,
`rowsEstimated`, `rowsCheckpoints` — and it is written **whether or not that number was the tier that answered**.
That is deliberate: the only ground truth for how good the derivation is, is the provider's count of a request that
actually went out, and on the runs where an exact count won, that count exists. So `ledgerSkew` compares
`ledger.tokens` with `usage.input + cacheRead + cacheWrite` for the same attempt, and the attempt line prints the
result as a signed `ledger=+0.4%` beside `est=`/`src=` — three states again, in the same idiom as `cut=`: nothing
printed when the record carries no `ledger` object (the derivation declined, or this record predates it),
`ledger=-` when it derived a number but no reply came back to compare it with, and the signed percent when both
sides exist.

The 5% band is measured, not chosen for symmetry: against three real stage-1 requests the derivation came out
0.96%, 0.01% and 0.26% off what the endpoint charged — on the middle of those, every count in the span had expired
(`staleAnchors: 14`), the shipped tiers fell back to whole-body chars/4, and that fallback recorded +8.8% against
the same provider count. Move it with `--ledger-estimate-skew <fraction>`.

`LEDGER` aggregates that comparison: how many runs carried a ledger number and how many of those had a provider
count to compare with, the mean and worst absolute skew (naming the run), how many breached the band, and the mean
share of the ledger's own number that was chars/4 rather than counted. The section prints only when at least one
record carries the fields, because an older trace file must not grow an empty table that reads as a measurement of
nothing. The `est` share matters on its own: it is 0.3% of a deep session's stage-1 request and 91% of an
early-session range, and the derivation refuses bodies whose share exceeds half rather than pricing them.

The retired reading of these numbers, recorded so nobody resurrects it: chars/4 was said to run +40% mean error
(+93% worst) against the anchor's +2%. Both figures were real, and the gap was mostly an artifact of charging
_stored_ rows - a tool result's `details` holds a second full copy of a truncated `read`, which no provider ever
receives - rather than of chars/4 being a poor proxy. Over the wire shape the same heuristic measured -0.7% mean
on the session that had shown +42.2%. The counts still rank above it because they are measurements, but not
because the guess was 40% blind.

## What the flags mean

Each flag names a failure already observed in a live trace, so the report can point at the wrong runs instead
of leaving the numbers to be compared by eye. They are thresholds, not verdicts — `--min-chars`,
`--inflate-factor`, and `--ledger-estimate-skew` exist to move them.

| Flag                       | What it means                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `prefix-unusable`          | a comparable reference depth disagreed with our rebuild, so stage 1 could not reuse the provider's cached prefix                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `no-prefix-reference`      | no observation covered this branch, so the cache question is unanswered. Not a failure - and the case the old code reported as one                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `chain-empty`              | no persisted row for this session was readable, so nothing in the retained file records a request for it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `chain-off-branch`         | the chain holds entries and none of them sit on the current branch, so a fork or a rewind is why there is no reference                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `chain-incomparable`       | on-branch requests exist but the shape filter rejected them all, and the detail names the gate: `4 on-branch entries, 4 rejected by the system prompt and 0 by the tool set`. It used to assert both halves at once, which read as a tool-set change on a run whose tool set never moved; a record written before the counters says so instead                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `system-prompt-drift`      | ours and the parent's system prompts hash apart at the same length — drift no size figure can show                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `prefix-uncomparable`      | a reference passed the shape filter but nothing in it shared a depth with the truncated span, so nothing could be compared. Suppressed when the shape gate emptied the funnel, because `chain-incomparable` names that cause and a second flag for one fact double-counts it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `cut-not-found`            | pi's `firstKeptEntryId` named no entry on this branch, so stage 1 read the whole transcript including the tail that survives. The attempt record's `cutFound` is the only place this is knowable - an uncut span and a merely long span carry the same counts - and the line prints `cut=found` / `cut=missing` / `cut=unrecorded` so a pre-field record cannot read as a found cut                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `span-not-truncated`       | our span was no shorter than the reference that verified it, the shape a missing cut point leaves behind. An inference from a depth comparison, so it stays quiet whenever `cutFound` says the same thing directly, and a shallow reference can print it with nothing wrong                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `degenerate-native-output` | stage 1 was accepted but answered with far too little text — the shape of a model replying about the instruction rather than to it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `degenerate-final-summary` | what got persisted is implausibly small, whatever route produced it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `compaction-abandoned`     | the run stopped on a cause no request can fix (quota, rejected credentials, rate limit); not a handover, so `fell-back` stays clear                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `refused-cut-shipped`      | the walk refused core's boundary as inadmissible and the run shipped it anyway, because nothing earlier was admissible. Not a contradiction - it is the abstain path, and the consequence the ledger-backed walk exists to remove: too little retained history, or a request the window cannot take. `moved-without-cause` in INVARIANTS is the contradiction, and it is one-directional on purpose: a cause with no move is legitimate, a move with no cause is not.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `retry-exhausted`          | a transient failure used its whole backoff budget and still failed, so the provider was unwell longer than we wait                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `summary-truncated`        | stage 2 hit the output limit, so the persisted summary is missing its tail. Reading the text cannot tell this from a brief complete answer                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `reduce-inflated`          | stage 2's output is many times larger than the checkpoint it was handed, so it wrote a new summary instead of merging one                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `cache-read-zero`          | a large fresh prompt with no cache read: the span was paid for again                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `decode-divergence`        | our rebuild and the credited reference agree on body _keys_ but differ on a value that moves the prefix on templating servers (thinking switch, effort, image count)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `blocks-dropped`           | transcript blocks left out of the reduce request at the serialization cap, so the summary covers a truncated view                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `fell-back`                | the run ended in `core-default`, `cancelled`, or with no outcome record at all                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `estimate-skew`            | our estimate of a request and the tokens the provider counted for **that same request** (`usage.input + cacheRead + cacheWrite`) disagree by more than the band that number's own method can support: **5%** when `estimateSource` is `exact-cut` - the provider's count of the very body being sent, so a real disagreement means the two bodies differ (check `skippedEnt`, and whether a fold or a model change sits inside the span) - **15%** for `usage-anchor` (worst measured error 1.1% across 11 real requests), **5%** for `head-ledger` (worst measured error 0.96% across three real stage-1 requests), and **50%** for whole-body chars/4. It used to compare the estimate with pi's whole live-context count, a different body once the span truncates, so one healthy pipeline printed -34% and the next +21%. For the two estimated tiers the detail names the direction: an estimate far above the count means the endpoint may have clipped the input (the silent-overflow shape pi's docs attribute to z.ai, MiMo, Ollama) or that the estimate over-reads the content; far below means the fit gate decided on a number too small |
| `ledger-skew`              | the head ledger's `ledger.tokens` and the provider's count of that same request disagree by more than **5%** (`--ledger-estimate-skew`), checked on every stage-1 attempt that carries the fields rather than only on the runs where the ledger tier answered. The derivation disagrees with a measurement of the very bytes that went out, so either the head was solved from a reply under a request shape that has since moved — a model, tool-set, or system-prompt change sits between that reply and this request, and pi records no row for its own prompt flipping — or the chars/4 share of the number was too large to trust. The detail prints the head, which reply it came from, and that share, so a breach is attributable rather than argued about. Absent fields produce nothing: a rotated or older trace file has no data, not a clean bill                                                                                                                                                                                                                                                                                         |

## Validation

`test/scripts/compaction-report.test.ts` feeds the script records written by the **real** recorder, so the
fixture cannot drift from the trace schema.

It also pins one **real** run: `test/fixtures/compaction-trace.healthy.jsonl` is a llama.cpp compaction for
the repository, captured raw on 2026-09-06 — real `cwd`, session id, instance, persisted chain rows, and both
`model_response` texts — with one normalization: the provider name is written as `llamacpp`, because the alias in
a given machine's provider config says nothing about pi's shape. The fixture it replaced was a sanitized extract (deterministic ids,
re-based timestamps, a synthetic checkpoint block, `model_response` dropped); the raw capture took its place
because the fields worth pinning now include the chain rows and the decode scalars, and because rebuilding stage
text from records is itself a behaviour to cover. The case the trimmed fixture stood for — a flag must not fire
because a record was _absent_ — is carried by the synthetic tests instead.

What the fixture asserts about a healthy run: `reference=chain`, `usable=true`, all 18 span depths verified
against a 31-message reference, `parameters` **empty** (our body sent no key pi's turn requests lacked and
dropped none they carried), zero flags, `referenceSource: observation`, `otherDisagreements: 0`, and 13 hash
rows that belong to no run.

All three captured fixtures predate the `ledger` fields, and one test walks every one of them to pin that
absence end to end: no `ledger=` on any stage line, no `LEDGER` section, no `ledger-skew` suspect, no
`tier-without-ledger` invariant, and `ledger: null` in `--json` - while those same files' stage-1 attempts do
carry a provider count to compare against, so the silence is tolerance rather than an empty run.
